{% block extends %}
{{ "{% extends 'base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block page_title_text %}" }}
    {{ "{{ '"~entity_singularized~".edit_title' | trans }}" }}
{{ "{% endblock %}" }}

{{ "{% block body %}" }}
    <div class="panel">
        <div class="panel-body">
            {{ '{{ form_start(edit_form) }}' }}
                {{ '{{ form_widget(edit_form) }}' }}
                <input type="submit" value="{{ "{{ 'button.update' | trans }}" }}" class="btn btn-primary" />
            {{ '{{ form_end(edit_form) }}' }}
        </div>
    </div>

{% set hide_edit, hide_delete = true, false %}
{{ include('crud/views/others/record_actions.html.twig.twig') -}}
{{ "{% endblock %}" }}
{% endblock body %}
